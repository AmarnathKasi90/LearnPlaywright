let retry = 0;

// do {
//     console.log("retry ", retry);
// } while (retry < 3) // Infinite Loop with 0

// do {
//     console.log("retry ", retry);
//     retry++;
// } while (retry < 3)
    
// do {
//     console.log("retry", retry);
//     retry++;
// } while(retry < 0)

// do {
//     console.log("retry", retry);
//     retry++;
// } while (retry > 0) // Infinite Loop with Number Increase