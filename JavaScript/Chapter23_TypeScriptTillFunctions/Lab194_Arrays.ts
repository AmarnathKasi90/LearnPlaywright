let nums: number[] = [1, 2, 3]
let names: Array<string> = ["Kasi", "Reddy", "Amarnath"]

nums.forEach(function (num) {
    console.log(num)
})

names.forEach(function (name) {
    console.log(name)
})

